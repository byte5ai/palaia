# palaia 3.0.0-rc2 — second v3 release candidate

**A test build to shake out `rc1`.** Everything from [`3.0.0-rc1`](https://github.com/byte5ai/palaia/releases/tag/v3.3.0.0-rc1) is here; this note only lists what changed since. Run it, break it, tell us.

> [!IMPORTANT]
> **This is a release candidate, not a final release.** It has not yet had an external security review or a usability pass with fresh eyes. Run it, break it, tell us — and keep backups. **palaia v2 remains the stable, supported product** until 3.0.0 is final.

## Try it

```bash
docker run -d --name palaia-hub \
  -p 8420:8420 \
  -v palaia_home:/data \
  --restart unless-stopped \
  --security-opt no-new-privileges:true --cap-drop ALL \
  --read-only --tmpfs /tmp --tmpfs /run \
  ghcr.io/byte5ai/palaia-hub:beta
```

Then open `http://<host>:8420` (on many home networks also `http://palaia.local`) and follow the setup wizard. Full options — Docker Compose, Synology, Raspberry Pi, VPS with Tailscale — are in the [install guide](https://github.com/byte5ai/palaia/blob/main/v3/deploy/README.md).

This tag publishes the container image as `ghcr.io/byte5ai/palaia-hub:beta` and `ghcr.io/byte5ai/palaia-hub:v3.3.0.0-rc2` (amd64 + arm64).

## New since rc1

**Memory & search**
- Search now tells you *how* it found each hit: per-hit provenance (full-text / vector / both) and a per-result `degraded` signal when semantic search was unavailable and the query fell back to text-only — so an agent can weigh the results instead of trusting them blindly.
- Smart Nudges: short, deterministic, rule-based guidance attached to a successful memory tool's output (no extra model call), e.g. a note when a search ran degraded. Error results are left untouched.

**Operations**
- `palaia doctor` — a whole-hub diagnose command: a check framework with real checks across the hub, run from the CLI, reporting problems in one place.
- Backup targets — a backup-destination abstraction with a built-in secret-safety invariant (a destination that isn't secret-safe is refused before a byte is written), plus a local-directory target end-to-end (CLI, REST `/api/backup/targets`, and `backup.target.*` events). More targets, scheduling and a dashboard panel are [tracked follow-ups](https://github.com/byte5ai/palaia/issues/438).

**Fixes**
- `claude mcp get` / `claude mcp list` no longer reports "Failed to connect" against a working OAuth profile — the protected-resource metadata now advertises the gateway mount URL as its RFC 9728 `resource`.
- Docs corrected: the nonexistent "recall" hook-event claim in how-it-works, and the plugin README's `memoryInject` default and search description.

**Under the hood**
- Supply chain: v3 workflow actions pinned by commit SHA, base images pinned by digest, Dependabot keeping both current; store manifests (TrueNAS, Umbrel) corrected.

## Preview, not yet active

A **Telegram connector** has landed in the codebase (inbound routing per bot/chat, outbound send tools, message-text redaction) but is **not yet wired into the running hub** — daemon wiring and a dashboard panel are the [remaining work](https://github.com/byte5ai/palaia/issues/439). It does nothing in this image yet; it ships early so its shape can be reviewed.

## Known gaps

Same as rc1, documented honestly in [`v3/docs/client-matrix-results.md`](https://github.com/byte5ai/palaia/blob/main/v3/docs/client-matrix-results.md); [`v3/RELEASING.md`](https://github.com/byte5ai/palaia/blob/main/v3/RELEASING.md) lists what still stands between this RC and 3.0.0.

## Feedback

Found a bug or something confusing? [Open an issue](https://github.com/byte5ai/palaia/issues). Security findings: please use [private vulnerability reporting](https://github.com/byte5ai/palaia/security/advisories/new).
