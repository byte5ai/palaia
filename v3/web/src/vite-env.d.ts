/// <reference types="vite/client" />

/** Injected by vite.config.ts's `define` from package.json — see
 * ../version.ts, the single place this is read from. */
declare const __APP_VERSION__: string;
