# palaia v3 web

Dashboard placeholder (Vite + React + TypeScript + Tailwind), established by
SPEC-001. No real UI yet — the dashboard design lands in SPEC-109/110.

```bash
npm ci
npm run dev        # Vite dev server
npm test           # vitest
npm run lint       # eslint
npm run typecheck  # tsc -b
npm run build      # production build
```
