"""The SPEC-502 hardening suite.

Each module here asserts one thing the threat model
(``v3/docs/security/threat-model.md``) claims about the hub as built. A claim
in that document with no test in this directory is a claim that can rot.
"""
